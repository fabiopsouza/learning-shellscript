" Execução do gerenciador de plugins
execute pathogen#infect()

" Remapear atalho do emment (Normalmente os plugins permitem alterar o atalho)
let g:user_emmet_leader_key=','

" Esquema de cores escuro
colorscheme industry

" Tamanho de identação
set tabstop=2

" Identifica o tipo de arquivo e identa
filetype plugin indent on

" Colorir o editor
syntax on

" Deixar coerente identação com tamanho do TAB
set shiftwidth=2

"	Comportamento usual do backspace
set backspace=2

" Numerar linhas
set number

" Calcular diferença das linhas
set relativenumber

" Feedback enquanto escreve find em arquivo
set incsearch

" Desquate na busca
set hlsearch

" Salvar na codificação desejada
set fileencoding=iso-8859-1

" Visualizar na codificação desejada
set-encoding=iso-8859-1

" Usar espaços no lugar de tab
set expandtab=2
